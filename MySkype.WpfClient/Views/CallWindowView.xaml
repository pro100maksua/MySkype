<Window x:Class="MySkype.WpfClient.Views.CallWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MySkype.WpfClient.Views"
        mc:Ignorable="d"
    Title="CallWindowView" ResizeMode="NoResize" Width="700" Height="550">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="VisibilityConverter"/>
    </Window.Resources>

    <StackPanel  Background="DarkSlateGray">
        <Ellipse Margin="0,10" Height="100" Width="100" HorizontalAlignment="Center">
            <Ellipse.Fill>
                <ImageBrush ImageSource="{Binding Friend.Avatar.Bitmap}"/>
            </Ellipse.Fill>
        </Ellipse>

        <TextBlock HorizontalAlignment="Center"  FontSize="24" Text="{Binding Friend.FullName}"/>
        <TextBlock Visibility="{Binding Started, Converter={StaticResource VisibilityConverter}}" HorizontalAlignment="Center"  Text="{Binding Duration}"/>

        <StackPanel Margin="0,20" HorizontalAlignment="Center" Orientation="Horizontal">
            <Button Name="ToggleMuteButton" Margin="10,10,10,10" Width="40" Height="40">
                <Button.Template>
                    <ControlTemplate>
                        <Ellipse Height="40" Width="40">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="/Assets/unmute.png"/>
                            </Ellipse.Fill>
                        </Ellipse>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Name="ToggleMicroButton" Margin="10,10,10,10" Width="40" Height="40">
                <Button.Template>
                    <ControlTemplate>
                        <Ellipse Height="40" Width="40">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="/Assets/micro.png"/>
                            </Ellipse.Fill>
                        </Ellipse>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Name="ToggleVideoButton" Margin="10,10,10,10" Width="40" Height="40">
                <Button.Template>
                    <ControlTemplate>
                        <Ellipse Height="40" Width="40">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="/Assets/vcall.png"/>
                            </Ellipse.Fill>
                        </Ellipse>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Command="{Binding CloseCommand}" Margin="10,10,10,10" Width="40" Height="40">
                <Button.Template>
                    <ControlTemplate>
                        <Ellipse Height="40" Width="40">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="/Assets/endCall.png"/>
                            </Ellipse.Fill>
                        </Ellipse>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </StackPanel>
</Window>